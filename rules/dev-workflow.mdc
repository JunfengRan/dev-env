---
description: 开发工作流偏好 — 计划、实现、验证的节奏
alwaysApply: true
---

# 开发工作流

## 做事之前先搞清楚

不确定的地方先问，不要自己猜然后做错了再返工。尤其是：
- 用户旅程和交互细节
- 第三方库的行为（去看源码或文档，不要靠记忆）
- API、功能、能力必须在源码或文档中确认存在
- 删除或重构已有功能前，确认用户真的不需要了

## 计划文件

- 计划文件放在 `.cursor/plans/` 目录
- 实现时**不要修改计划文件本身**
- 按计划中的 todo 顺序推进，一个做完标 completed 再做下一个

## 响应速度

- 浏览器操作每步等待不超过 3 秒
- 卡住时说明卡在哪，让用户决定怎么继续

## 改动方式与实现质量

- 改动前先梳理既有代码边界与职责，优先小范围修改
- 非平凡行为先与用户对齐方案再编码

## 验证方式

功能做完后，优先用浏览器实际操作验证，而不是只看代码。

## 深度研究 vs 快速实现

- 研究、选型、竞品对比、决策文档 → `deep-research` skill + Spec 状态机
- 功能实现、bug 修复 → 本规则快速路径

## 启发式 brainstorm（可选）

- 新功能/架构意图未对齐时，可建议使用 `brainstorm` skill（一次一问、2–3 方案取舍）
- **非硬门禁**：用户已有明确计划、或改动琐碎、或明确拒绝时跳过
- 不进入 research Spec 状态机；需要落盘时交给 `write-idea-docs` 或写入 research brief 草稿

## 不做多余的事

- 用户说"不需要"的功能就不做
- 不要主动加用户没提到的功能
- 不要写文档，除非用户让写

## 代理与 sudo

- `sudo` 会清除代理环境变量；公司代理下用 `sudo -E` 或显式传递 `HTTPS_PROXY`/`HTTP_PROXY`

## Git commit（Agent 终端）

Agent 终端执行 git commit 时，先 `unset CI` 或 `$env:CI=$null`，避免 hook 误判。
